# 2026-08-05 공부 기록

## 오늘 한 것

- 워게임: [baby-linux (#837)](https://dreamhack.io/wargame/challenges/837)
- 리눅스 기본 명령어 정리 (파일·디렉터리·읽기·필터 우회용 glob)

---

## 리눅스 기본 명령어

웹 셸·SSH·Docker 컨테이너 안에서 자주 씀.  
경로: `.` = 현재, `..` = 상위, `/` = 루트, `~` = 홈.

### 어디에 있나 / 이동

| 명령 | 의미 | 예 |
|------|------|-----|
| `pwd` | 현재 경로 출력 | `pwd` |
| `ls` | 목록 | `ls`, `ls -la` |
| `ls 경로` | 그 경로 목록 | `ls ./dream/hack/hello/` |
| `cd 경로` | 이동 | `cd /tmp`, `cd ..` |
| `cd` / `cd ~` | 홈으로 | |

| `ls` 옵션 | 의미 |
|-----------|------|
| `-l` | 자세히 (권한, 크기, 날짜) |
| `-a` | 숨김 파일(`.` 시작) 포함 |
| `-la` / `-al` | 둘 다 |

### 파일 내용

| 명령 | 의미 | 예 |
|------|------|-----|
| `cat 파일` | 전체 출력 | `cat hint.txt` |
| `head 파일` | 앞부분 | `head -n 20 f` |
| `tail 파일` | 뒷부분 | `tail -n 20 f` |
| `less 파일` | 페이지 단위 (q 종료) | `less big.log` |
| `wc -l 파일` | 줄 수 | |

### 찾기 / 검색

| 명령 | 의미 | 예 |
|------|------|-----|
| `find 경로 -name '이름'` | 파일 찾기 | `find . -name '*.txt'` |
| `grep 패턴 파일` | 내용 검색 | `grep DH flag.txt` |
| `which 명령` | 실행 파일 위치 | `which cat` |

### 파일·디렉터리 만들기 / 복사 / 삭제

| 명령 | 의미 | 예 |
|------|------|-----|
| `touch 파일` | 빈 파일 생성/시간 갱신 | `touch a.txt` |
| `mkdir 디렉터리` | 폴더 생성 | `mkdir work` |
| `mkdir -p a/b/c` | 중간 경로까지 생성 | |
| `cp 원본 대상` | 복사 | `cp a.txt b.txt` |
| `cp -r 폴더 대상` | 폴더 복사 | |
| `mv 원본 대상` | 이동/이름 변경 | `mv a.txt b.txt` |
| `rm 파일` | 삭제 | `rm a.txt` |
| `rm -r 폴더` | 폴더 삭제 | 주의 |
| `rmdir 빈폴더` | 빈 디렉터리만 삭제 | |

### 권한·프로세스·기타

| 명령 | 의미 |
|------|------|
| `chmod` | 권한 변경 (`chmod +x script.sh`) |
| `id` / `whoami` | 현재 사용자 |
| `ps` / `ps aux` | 프로세스 목록 |
| `kill PID` | 프로세스 종료 |
| `echo 문자열` | 출력 / 리다이렉트 `echo hi > f` |
| `man 명령` | 매뉴얼 (`man ls`) |
| `clear` | 화면 지우기 |
| `history` | 명령 기록 |

### 셸 글로브 (와일드카드) — 필터 우회에 자주 씀

| 기호 | 의미 | 예 |
|------|------|-----|
| `*` | 아무 문자열 (빈 문자열 포함) | `*.txt`, `hello/*` |
| `?` | 아무 문자 **1개** | `????.txt` → 4글자 이름 |
| `[abc]` | a 또는 b 또는 c | `file[12].txt` |

명령 문자열에 `flag` 가 들어가면 막는 필터가 있을 때:

```bash
# 안 됨
cat ./dream/hack/hello/flag.txt

# 됨 (파일명에 flag 를 직접 안 씀)
cat ./dream/hack/hello/*
cat ./dream/hack/hello/????.txt
```

`*` / `?` 는 **셸이 먼저** 실제 파일 이름으로 펼친 뒤 명령에 넘김.

### 리다이렉트·파이프 (기초)

| 문법 | 의미 |
|------|------|
| `cmd > f` | 출력을 파일로 (덮어쓰기) |
| `cmd >> f` | 이어 쓰기 |
| `cmd < f` | 입력을 파일에서 |
| `cmd1 \| cmd2` | cmd1 출력 → cmd2 입력 |

---

## 워게임: baby-linux (#837)

- 링크: https://dreamhack.io/wargame/challenges/837
- 카테고리: Beginner / Web (리눅스 명령 실행 서비스)
- 정리: `war_game/baby-linux_837/`

### 문제 요지

웹에서 리눅스 명령을 실행하는 서비스.  
`flag.txt` 를 찾아 내용을 출력하면 플래그 (`DH{...}`).

### 필터

소스에 대략:

```python
if 'flag' in cmd:
    return ... 'No!'
```

명령 문자열에 **`flag` 가 포함되면** 실행 거부.

| 안 됨 | 됨 |
|-------|-----|
| `cat flag.txt` | `cat ./dream/hack/hello/*` |
| `cat ./dream/hack/hello/flag.txt` | `cat ./dream/hack/hello/????.txt` |

### 풀이 순서

1. 서버 생성 → 브라우저 접속  
2. `ls` — 파일 목록 (`hint.txt` 등)  
3. `cat hint.txt` — 경로 힌트 `./dream/hack/hello/`  
4. `ls ./dream/hack/hello/` — `flag.txt` 존재 확인  
5. `cat ./dream/hack/hello/*` — 필터 우회 후 내용 출력  
6. `DH{...}` 제출  

### 치트시트

```text
ls
cat hint.txt
ls ./dream/hack/hello/
cat ./dream/hack/hello/*
```

### #873 phpreg 와 비교

| | #873 | #837 |
|--|------|------|
| 환경 | PHP `system()` | 웹 bash |
| 막힘 | `flag` in cmd | `flag` in cmd |
| 우회 | `cat ../dream/????.txt` | `cat ./dream/hack/hello/*` |

둘 다 **glob으로 파일명을 직접 쓰지 않기**.

---

## 오늘 배운 것 (한 줄)

1. `ls` / `cd` / `cat` / `pwd` 로 탐색·읽기
2. `hint.txt` 같은 힌트 파일 먼저 확인
3. 문자열 필터(`flag`)는 `*` / `?` 글로브로 우회 가능
4. 글로브는 셸이 파일 이름으로 확장한 뒤 명령에 전달됨
